


def fetch_latest(arch_ref, version_id=None, run_id=None):
  """Fetch the latest results from jenkin for a given jenkins job.

  Most likely run as a polling loop to import data for our displays.

  """
  pass


def spawn_build(arch_ref, version_id=None, run_id=None):
  """Trigger a build in jenkins and start a polling loop for the results."""
  pass
